# Core (always required)
numpy>=1.24        # host-side arrays, memmap storage, reference/scoring math

# Compute backend (required) — CCO runs every matrix product on a GPU with
# PyTorch (CUDA or Apple MPS). There is no CPU or CuPy backend.
#   NVIDIA GPU (CUDA 12.x)  ->  pip install torch
#   Apple Silicon (MPS)     ->  pip install torch
torch>=2.1

# Optional, for running the test suite with pytest:
#   pip install pytest
